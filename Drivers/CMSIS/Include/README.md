# Include Directory

This directory contains CMSIS core and compiler headers for ARM Cortex-M microcontrollers.

## Purpose
- Provides standard interfaces for ARM Cortex-M cores and compiler compatibility.

---

For device-specific files, see the Device directory.